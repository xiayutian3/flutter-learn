import 'package:flutter/material.dart';

class HY404 extends StatelessWidget {
  const HY404({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("404页面"),
        ),
        body: Text('没有找到'),
    );
  }
}
